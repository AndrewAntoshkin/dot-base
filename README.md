# .base - AI Generation Platform 🚀

Масштабируемый сервис для генерации, редактирования, улучшения и обработки изображений с использованием AI моделей через Replicate.

## 📋 Содержание

- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Архитектура](#архитектура)
- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [API Документация](#api-документация)
- [Модели](#модели)
- [Deployment](#deployment)

## ✨ Возможности

### Действия с изображениями

1. **Создать (Create)** - Генерация изображений из текста
   - 8 моделей: SDXL Lightning, FLUX Schnell, Stable Diffusion, и др.

2. **Редактировать (Edit)** - Изменение существующих изображений
   - 5 моделей: Nano Banana, SeeDream 4, FLUX Kontext Pro, и др.

3. **Улучшить качество (Upscale)** - Увеличение разрешения
   - 3 модели: Real-ESRGAN, Clarity Upscaler, Recraft Crisp Upscale

4. **Удалить фон (Remove Background)**
   - 3 модели: Background Remover, BiRefNet, BRIA

### Ключевые особенности

- ⚡ **Масштабируемость** - Пул из 10 API токенов Replicate
- 🎨 **Pixel-perfect UI** - Дизайн на основе Figma макетов
- 🔐 **Telegram авторизация** - Доступ по никнейму Telegram
- 💾 **Полная история** - Все генерации сохраняются в БД
- 🚀 **Быстрые генерации** - Параллельная обработка запросов
- 📊 **Детальная аналитика** - Время обработки, статусы, ошибки

## 🛠 Технологический стек

### Frontend
- **Next.js 14** (App Router)
- **TypeScript** - Строгая типизация
- **Tailwind CSS** - Утилитарные стили
- **Shadcn/ui** - Компоненты UI
- **React Query** - Управление данными
- **Zustand** - State management

### Backend
- **Next.js API Routes** - REST API
- **Supabase** - PostgreSQL + Storage + Auth
- **Replicate SDK** - Интеграция с AI моделями
- **BullMQ + Redis** - Очереди задач (опционально)

### Infrastructure
- **Vercel** - Деплой и хостинг
- **Supabase Cloud** - Управляемая БД
- **Upstash Redis** - Serverless Redis

## 🏗 Архитектура

```
┌─────────────────┐
│   Next.js App   │
│   (Frontend +   │
│   API Routes)   │
└────────┬────────┘
         │
         ├──────────> Supabase (PostgreSQL)
         │            - users
         │            - generations
         │            - replicate_tokens
         │
         ├──────────> Supabase Storage
         │            - Generated images
         │            - User uploads
         │
         └──────────> Replicate API
                      - Token Pool (10 tokens)
                      - Round-robin балансировка
                      - Автоматический retry
```

### Token Pool System

Система управления пулом API токенов для распределения нагрузки:

```typescript
// Автоматический выбор наименее загруженного токена
const { token, tokenId } = await tokenPool.getNextToken();

// Ротация токенов при ошибках
if (error) {
  await tokenPool.reportTokenError(tokenId, error.message);
}
```

## 📦 Установка

### Требования

- Node.js 18+
- npm или yarn
- Supabase проект (бесплатный аккаунт)
- 10 API токенов Replicate

### Шаги установки

1. **Клонировать репозиторий**
```bash
cd dot-base
```

2. **Установить зависимости**
```bash
npm install
```

3. **Настроить переменные окружения**
```bash
cp .env.example .env.local
```

Заполните `.env.local`:
```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Replicate (10 токенов)
REPLICATE_API_TOKENS=token1,token2,token3,...

# Redis (опционально)
REDIS_URL=redis://localhost:6379

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-random-secret

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-bot-token
```

4. **Настроить Supabase**

Выполните SQL из `supabase/schema.sql` в Supabase SQL Editor:

```bash
# Или используйте Supabase CLI
supabase db push
```

5. **Запустить в режиме разработки**
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## ⚙️ Настройка

### Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL схему из `supabase/schema.sql`
3. Создайте Storage bucket `generations` с публичным доступом
4. Скопируйте URL и ключи в `.env.local`

### Replicate Tokens

Вам нужно 10 API токенов Replicate для оптимальной производительности:

1. Создайте 10 аккаунтов на [replicate.com](https://replicate.com)
2. Получите API токены из Settings
3. Добавьте их в `.env.local` через запятую:

```env
REPLICATE_API_TOKENS=r8_token1,r8_token2,...,r8_token10
```

Токены автоматически загрузятся в БД при первом запуске.

### Telegram Bot (опционально)

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен
3. Добавьте в `.env.local`

## 🎯 Использование

### Создание генерации

1. Выберите **Действие** (Create, Edit, Upscale, Remove BG)
2. Выберите **Модель** из списка доступных
3. Заполните **Настройки** (prompt, параметры)
4. Нажмите **"Создать"**

### Просмотр результатов

- Результаты отображаются в реальном времени
- Автоматическое обновление статуса каждые 2 секунды
- Скачивание готовых изображений

### История генераций

- Все генерации сохраняются в истории
- Фильтрация по действию и статусу
- Пагинация (12 на странице)

## 📚 API Документация

### Endpoints

#### POST `/api/generations/create`
Создать новую генерацию

**Request:**
```json
{
  "action": "create",
  "model_id": "flux-schnell",
  "prompt": "A beautiful sunset over mountains",
  "settings": {
    "aspect_ratio": "16:9",
    "num_outputs": 1
  }
}
```

**Response:**
```json
{
  "id": "uuid",
  "prediction_id": "replicate-prediction-id",
  "status": "processing"
}
```

#### GET `/api/generations/:id`
Получить статус генерации

**Response:**
```json
{
  "id": "uuid",
  "status": "completed",
  "output_urls": ["https://..."],
  "processing_time_ms": 5230
}
```

#### GET `/api/generations/list`
Список генераций пользователя

**Query params:**
- `page` - номер страницы (default: 1)
- `limit` - кол-во на странице (default: 20)
- `action` - фильтр по действию
- `status` - фильтр по статусу

#### GET `/api/models/list`
Список доступных моделей

**Query params:**
- `action` - фильтр по действию

## 🤖 Модели

### Создать изображение (8 моделей)

| Модель | Описание | Скорость | Качество |
|--------|----------|----------|----------|
| SDXL Lightning | Быстрая генерация за 4 шага | ⚡⚡⚡ | ⭐⭐⭐ |
| FLUX Schnell | Локальная разработка | ⚡⚡⚡ | ⭐⭐⭐⭐ |
| Stable Diffusion | Классика | ⚡⚡ | ⭐⭐⭐ |
| Nano Banana | Google Gemini 2.5 | ⚡⚡ | ⭐⭐⭐⭐ |
| SeeDream 4 | До 4K разрешения | ⚡⚡ | ⭐⭐⭐⭐⭐ |
| Recraft V3 | Контроль стиля | ⚡⚡ | ⭐⭐⭐⭐ |
| Imagen 4 | Google высокое качество | ⚡ | ⭐⭐⭐⭐⭐ |
| Ideogram V3 Turbo | Быстрая версия | ⚡⚡⚡ | ⭐⭐⭐⭐ |

### Улучшить качество (3 модели)

| Модель | Масштаб | Особенности |
|--------|---------|-------------|
| Real-ESRGAN | 2x, 4x | Универсальный апскейлер |
| Clarity Upscaler | 1-4x | Портреты, лица, продукты |
| Recraft Crisp | 2x, 4x | Четкое увеличение |

### Редактировать (5 моделей)

| Модель | Тип редактирования |
|--------|-------------------|
| Nano Banana | Общее редактирование |
| SeeDream 4 | Точное редактирование |
| FLUX Kontext Pro | Контекстное редактирование |
| Multi-Image Kontext | Несколько изображений |
| FLUX Fill Pro | Inpainting, заполнение |

### Удалить фон (3 модели)

Все модели удаляют фон с высокой точностью.

## 🚀 Deployment

### Vercel (рекомендуется)

1. **Push на GitHub**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin your-repo-url
git push -u origin main
```

2. **Подключить к Vercel**
- Импортируйте проект на [vercel.com](https://vercel.com)
- Добавьте переменные окружения
- Deploy!

3. **Настроить Webhook**
После деплоя обновите `NEXTAUTH_URL` на production URL:
```env
NEXTAUTH_URL=https://your-app.vercel.app
```

### Docker (опционально)

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

```bash
docker build -t dot-base .
docker run -p 3000:3000 --env-file .env.local dot-base
```

## 📊 Мониторинг

### Token Pool Status

Проверить статус токенов:
```sql
SELECT 
  id,
  is_active,
  request_count,
  error_count,
  last_used_at
FROM replicate_tokens
ORDER BY request_count ASC;
```

### Generation Statistics

```sql
SELECT 
  action,
  status,
  COUNT(*) as count,
  AVG(processing_time_ms) as avg_time
FROM generations
WHERE created_at > NOW() - INTERVAL '24 hours'
GROUP BY action, status;
```

## 🔧 Troubleshooting

### Проблемы с токенами Replicate

Если токен не работает:
```sql
UPDATE replicate_tokens 
SET is_active = false 
WHERE id = <token_id>;
```

### Ошибки генерации

Проверьте логи:
```bash
# Vercel
vercel logs

# Local
npm run dev
```

## 📝 Roadmap

### Phase 1 (MVP) ✅
- [x] Генерация изображений (8 моделей)
- [x] Редактирование (5 моделей)
- [x] Апскейлинг (3 модели)
- [x] Удаление фона (3 модели)
- [x] Telegram авторизация
- [x] Token Pool система

### Phase 2 (Q1 2025)
- [ ] Видео генерация (Sora, Runway, Pika)
- [ ] Batch обработка
- [ ] API для внешних приложений
- [ ] Webhook уведомления

### Phase 3 (Q2 2025)
- [ ] Audio генерация
- [ ] 3D модели
- [ ] Multi-modal (text + image + audio)

## 🤝 Contributing

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 📄 License

MIT

## 👥 Team

Created by [Your Name]

## 🙏 Acknowledgments

- [Replicate](https://replicate.com) - AI модели
- [Supabase](https://supabase.com) - Backend as a Service
- [Vercel](https://vercel.com) - Деплой платформа
- [shadcn/ui](https://ui.shadcn.com) - UI компоненты

---

**Важно!** Не забудьте обновить CHANGELOG.md при каждом значимом изменении! [[memory:9896586]]


