# 🚀 Quick Start - .base

Запустите проект за 10 минут!

## ⚡ Быстрый старт (локально)

### Шаг 1: Клонирование (если еще не сделано)

```bash
cd dot-base
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Скопируйте URL и ключи
3. Выполните SQL из `supabase/schema.sql`

### Шаг 4: Переменные окружения

Создайте `.env.local`:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Replicate (минимум 1 токен для теста)
REPLICATE_API_TOKENS=r8_YOUR_TOKEN_HERE

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=your-bot-token
```

### Шаг 5: Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) 🎉

---

## 📝 Детальная настройка

### 1. Получение Replicate токенов

Для полноценной работы нужно 10 токенов:

```bash
# 1. Зарегистрируйтесь на replicate.com
# 2. Settings → API tokens
# 3. Create token
# 4. Скопируйте токен (r8_...)
```

Добавьте все 10 токенов через запятую:
```env
REPLICATE_API_TOKENS=r8_token1,r8_token2,r8_token3,...
```

### 2. Настройка Supabase Storage

В Supabase Dashboard:

1. Storage → New Bucket
2. Name: `generations`
3. Public: ✅ Yes
4. Save

### 3. Добавление тестового пользователя

В Supabase SQL Editor:

```sql
INSERT INTO users (telegram_username, is_active, credits)
VALUES ('testuser', true, 100);
```

---

## 🧪 Тестирование

### Проверка установки

```bash
# Проверка зависимостей
npm list

# Проверка типов
npm run type-check

# Проверка линтера
npm run lint
```

### Проверка API

```bash
# Проверка моделей
curl http://localhost:3000/api/models/list

# Должен вернуть JSON с моделями
```

### Тестовая генерация

1. Откройте http://localhost:3000
2. Выберите **"Создать изображение"**
3. Выберите модель **"FLUX Schnell"**
4. Введите prompt: `"A beautiful sunset over mountains"`
5. Нажмите **"Создать"**
6. Ждите результат (~5-10 секунд)

---

## 🐛 Troubleshooting

### Ошибка: "No available Replicate tokens"

**Решение:** Проверьте токены в БД

```sql
SELECT * FROM replicate_tokens;
```

Если пусто - вставьте токены:

```sql
INSERT INTO replicate_tokens (token) 
VALUES ('r8_YOUR_TOKEN_HERE');
```

### Ошибка: "Unauthorized"

**Решение:** Проверьте Supabase ключи в `.env.local`

### Ошибка: Cannot find module

**Решение:** 
```bash
rm -rf node_modules
rm package-lock.json
npm install
```

### Генерация не создается

**Решение:**
1. Проверьте баланс Replicate аккаунта
2. Проверьте логи в консоли
3. Проверьте Network tab в DevTools

---

## 📚 Следующие шаги

После успешного запуска:

1. ✅ **Прочитайте README.md** - полная документация
2. ✅ **Изучите MODELS.md** - все доступные модели
3. ✅ **Настройте Telegram бота** - для авторизации
4. ✅ **Добавьте 10 токенов** - для масштабирования
5. ✅ **Настройте мониторинг** - отслеживание использования

---

## 🚀 Production Deployment

Когда будете готовы к деплою:

1. Прочитайте **DEPLOYMENT.md** - пошаговая инструкция
2. Подготовьте production environment
3. Задеплойте на Vercel
4. Настройте мониторинг

---

## 💡 Полезные команды

```bash
# Разработка
npm run dev          # Запуск dev сервера
npm run build        # Сборка production
npm run start        # Запуск production сервера

# Проверки
npm run lint         # ESLint проверка
npm run type-check   # TypeScript проверка

# Очистка
rm -rf .next         # Очистить кеш Next.js
rm -rf node_modules  # Удалить зависимости
```

---

## 📞 Помощь

Если возникли проблемы:

1. Проверьте [README.md](./README.md)
2. Проверьте [DEPLOYMENT.md](./DEPLOYMENT.md)
3. Проверьте логи в консоли
4. Проверьте Supabase Dashboard
5. Проверьте Replicate Dashboard

---

## 🎯 Структура проекта

```
dot-base/
├── app/                    # Next.js App Router
│   ├── api/               # API endpoints
│   ├── history/           # История генераций
│   ├── result/            # Просмотр результата
│   └── page.tsx           # Главная страница
├── components/            # React компоненты
│   ├── ui/               # UI компоненты
│   └── *.tsx             # Feature компоненты
├── lib/                   # Библиотеки
│   ├── supabase/         # Supabase интеграция
│   ├── replicate/        # Replicate интеграция
│   └── models-config.ts  # Конфигурация моделей
├── supabase/             # Supabase файлы
│   └── schema.sql        # SQL схема
├── public/               # Статические файлы
└── *.md                  # Документация
```

---

## ✅ Checklist первого запуска

- [ ] Node.js 18+ установлен
- [ ] npm install выполнен
- [ ] Supabase проект создан
- [ ] SQL схема выполнена
- [ ] Storage bucket создан
- [ ] Replicate токен получен
- [ ] .env.local настроен
- [ ] npm run dev запущен
- [ ] Страница открывается
- [ ] Тестовая генерация работает

---

**Готово!** Теперь вы можете начать разработку! 🎉

Для production деплоя читайте [DEPLOYMENT.md](./DEPLOYMENT.md)








