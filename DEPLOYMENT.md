# Deployment Guide - .base

Пошаговая инструкция по деплою на production.

## 📋 Pre-deployment Checklist

- [ ] Supabase проект создан и настроен
- [ ] 10 Replicate API токенов получены
- [ ] Telegram бот создан (опционально)
- [ ] GitHub репозиторий создан
- [ ] Переменные окружения подготовлены

## 🗄️ Шаг 1: Настройка Supabase

### 1.1 Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "New Project"
3. Выберите организацию и введите данные:
   - **Name**: dot-base-prod
   - **Database Password**: сгенерируйте надежный пароль
   - **Region**: выберите ближайший регион
   - **Pricing Plan**: Pro (для production)

### 1.2 Выполнение SQL схемы

1. Откройте SQL Editor в Supabase Dashboard
2. Создайте новый запрос
3. Скопируйте содержимое файла `supabase/schema.sql`
4. Выполните запрос
5. Проверьте, что все таблицы созданы:
   - users
   - generations
   - replicate_tokens

### 1.3 Настройка Storage

1. Перейдите в раздел Storage
2. Создайте новый bucket:
   - **Name**: `generations`
   - **Public**: Yes (для доступа к изображениям)
3. Настройте политики доступа:

```sql
-- Политика для загрузки (только авторизованные)
CREATE POLICY "Authenticated users can upload"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'generations');

-- Политика для чтения (публичная)
CREATE POLICY "Public can read"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'generations');
```

### 1.4 Получение ключей

1. Перейдите в Settings → API
2. Скопируйте:
   - **Project URL**: `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key**: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key**: `SUPABASE_SERVICE_ROLE_KEY` (⚠️ секретный!)

## 🤖 Шаг 2: Настройка Replicate токенов

### 2.1 Получение токенов

Вам нужно 10 API токенов для оптимальной производительности:

1. Создайте 10 аккаунтов на [replicate.com](https://replicate.com)
   - Используйте разные email адреса
   - Можно использовать alias'ы Gmail (+1, +2, etc.)

2. Для каждого аккаунта:
   - Войдите в Settings
   - Перейдите в API tokens
   - Создайте новый токен
   - Скопируйте токен (начинается с `r8_`)

3. Добавьте токены в БД:

```sql
-- Вставка токенов (замените на реальные)
INSERT INTO replicate_tokens (token) VALUES
  ('r8_YOUR_TOKEN_1_HERE'),
  ('r8_YOUR_TOKEN_2_HERE'),
  ('r8_YOUR_TOKEN_3_HERE'),
  ('r8_YOUR_TOKEN_4_HERE'),
  ('r8_YOUR_TOKEN_5_HERE')
ON CONFLICT (token) DO NOTHING;
```

### 2.2 Мониторинг использования

Настройте алерты для мониторинга:
- Лимиты токенов
- Частота ошибок
- Баланс аккаунтов

## 🤖 Шаг 3: Telegram Bot (опционально)

### 3.1 Создание бота

1. Напишите [@BotFather](https://t.me/botfather)
2. Команда `/newbot`
3. Следуйте инструкциям:
   - Название бота: `.base AI Generator`
   - Username: `dotbase_ai_bot`
4. Получите токен

### 3.2 Настройка Web App

```bash
# Установите команды для бота
/setcommands

start - Запустить генератор
help - Помощь
```

## 🚀 Шаг 4: Vercel Deployment

### 4.1 Подготовка репозитория

```bash
# Инициализация git (если еще не сделано)
git init
git add .
git commit -m "Initial commit: .base AI Generation Platform"

# Создайте репозиторий на GitHub
# Затем:
git remote add origin https://github.com/username/dot-base.git
git branch -M main
git push -u origin main
```

### 4.2 Импорт в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте GitHub репозиторий
4. Настройте проект:
   - **Framework Preset**: Next.js
   - **Root Directory**: ./
   - **Build Command**: `npm run build`
   - **Output Directory**: .next

### 4.3 Настройка переменных окружения

В Vercel Settings → Environment Variables добавьте:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc... (⚠️ секретный!)

# Replicate - оставьте пустым, токены уже в БД
REPLICATE_API_TOKENS=

# NextAuth
NEXTAUTH_URL=https://your-app.vercel.app
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Telegram Bot
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
```

### 4.4 Deploy

1. Нажмите "Deploy"
2. Дождитесь завершения сборки
3. Перейдите по URL приложения

## ✅ Шаг 5: Post-deployment проверки

### 5.1 Проверка функциональности

- [ ] Главная страница загружается
- [ ] Выбор действия работает
- [ ] Выбор модели работает
- [ ] Форма настроек отображается
- [ ] Создание генерации работает
- [ ] Статус обновляется
- [ ] Результаты отображаются
- [ ] История генераций работает
- [ ] Скачивание работает

### 5.2 Проверка API endpoints

```bash
# Проверка списка моделей
curl https://your-app.vercel.app/api/models/list

# Должен вернуть JSON с моделями
```

### 5.3 Проверка токенов

```sql
-- В Supabase SQL Editor
SELECT 
  COUNT(*) as total_tokens,
  COUNT(*) FILTER (WHERE is_active = true) as active_tokens,
  SUM(request_count) as total_requests
FROM replicate_tokens;
```

## 🔧 Шаг 6: Настройка мониторинга

### 6.1 Vercel Analytics

1. В Vercel Dashboard включите:
   - **Speed Insights**
   - **Web Analytics**

### 6.2 Supabase Monitoring

1. Настройте Database Reports
2. Включите Performance Insights
3. Настройте Email алерты

### 6.3 Replicate Webhooks

Обновите webhook URL во всех генерациях:

```typescript
// В коде уже настроено
webhook: `${process.env.NEXTAUTH_URL}/api/webhook/replicate`
```

## 🔒 Шаг 7: Безопасность

### 7.1 Supabase RLS

Проверьте, что Row Level Security включен:

```sql
-- Проверка RLS
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';
```

Все таблицы должны иметь `rowsecurity = true`.

### 7.2 Rate Limiting

Настройте в Vercel:
- Edge Config для rate limiting
- IP blocking при необходимости

### 7.3 Environment Variables

⚠️ **НИКОГДА не коммитьте:**
- `.env.local`
- Service role keys
- API tokens

## 📊 Шаг 8: Мониторинг в Production

### 8.1 Dashboard для мониторинга

Создайте SQL view для статистики:

```sql
CREATE VIEW generation_stats AS
SELECT 
  DATE(created_at) as date,
  action,
  status,
  COUNT(*) as count,
  AVG(processing_time_ms) as avg_time,
  SUM(cost_credits) as total_cost
FROM generations
GROUP BY DATE(created_at), action, status;
```

### 8.2 Алерты

Настройте алерты для:
- Высокая частота ошибок (>5%)
- Медленные генерации (>30s)
- Исчерпание токенов
- Нехватка кредитов у пользователей

## 🔄 Шаг 9: Continuous Deployment

### 9.1 Автоматический деплой

Vercel автоматически деплоит при:
- Push в `main` → Production
- Push в другие ветки → Preview

### 9.2 Preview deployments

Каждый PR получает уникальный preview URL для тестирования.

## 📝 Шаг 10: Документация для пользователей

### 10.1 Первые пользователи

Добавьте первых пользователей в БД:

```sql
INSERT INTO users (telegram_username, is_active, credits)
VALUES 
  ('admin', true, 1000),
  ('user1', true, 100),
  ('user2', true, 100);
```

### 10.2 Инструкции для пользователей

Создайте простую инструкцию:
1. Перейдите на сайт
2. Войдите через Telegram
3. Выберите действие
4. Выберите модель
5. Создайте генерацию

## 🚨 Troubleshooting

### Проблема: Генерации не создаются

**Решение:**
1. Проверьте токены в БД
2. Проверьте логи Vercel
3. Проверьте баланс Replicate аккаунтов

### Проблема: Медленные генерации

**Решение:**
1. Проверьте, что используются все токены
2. Включите Redis для кеширования
3. Оптимизируйте настройки моделей

### Проблема: Ошибки авторизации

**Решение:**
1. Проверьте Supabase keys
2. Проверьте RLS policies
3. Проверьте CORS настройки

## ✅ Финальный чеклист

- [ ] Supabase настроен и работает
- [ ] 10 токенов Replicate добавлены
- [ ] Vercel деплой успешен
- [ ] Все environment variables настроены
- [ ] RLS включен и работает
- [ ] Storage bucket создан
- [ ] Webhook URL настроен
- [ ] Мониторинг настроен
- [ ] Тестовые генерации работают
- [ ] Документация обновлена

## 🎉 Готово!

Ваш сервис .base успешно задеплоен и готов к использованию!

**Production URL:** https://your-app.vercel.app

---

**Следующие шаги:**
- Добавить пользователей
- Настроить аналитику
- Мониторить использование токенов
- Планировать Phase 2 (видео)


